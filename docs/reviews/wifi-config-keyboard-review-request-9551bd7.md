# 评审申请书：菜单状态栏实时时钟（第十二次申请）

- **申请人**：Claude（pda2 现场调试，配合用户实测按键）
- **申请日期**：2026-08-16
- **关联分支**：`HD-V2-250915`
- **关联 commit**（本轮整改，与文件名对应）：
  - `9551bd7` — `pda2: show real local time in the menu status bar`
- **历史文档**（保留不覆盖）：前十一轮申请与结果文档见 `docs/reviews/`
- **硬件**：T-Deck-Pro HD-V2（V1.1，25-09-15 批次，COM5，**已连接、已烧录**）

---

## 1. 申请事由

用户观察：时间同步弹窗显示的已是正确时间，但首页状态栏时间仍显示 "10:19"。排查确认 `ui_deckpro.cpp::create0()` 中 `lv_label_set_text_fmt(menu_taskbar_time, "%02d:%02d", 10, 19)` 为**硬编码占位**，且任务栏定时器从未更新该标签。

## 2. 变更明细（`examples/pda2/ui_deckpro.cpp`，commit `9551bd7`）

- 创建时占位改为 `"--:--"`（未校时状态明确可见）
- `menu_taskbar_update_timer_cb()`（1s 周期）新增时间刷新：`time(nullptr) > 1700000000`（NTP 已同步）时用 `localtime_r` 取本地时间（CST-8），**仅在小时/分钟变化时**更新标签（避免墨水屏每秒重绘）；未同步时保持 `--:--`
- **电量显示核实为实时**：`ui_battery_27220_get_percent()`（BQ27220 真实读数），每 10s 刷新，充电状态每 1s 刷新——非写死，未改动

## 3. 验证状态

| 项目 | 状态 | 证据 |
|---|---|---|
| 编译 | ✅ 通过 | `pio run -e pda2` → SUCCESS |
| 烧录 | ✅ 完成 | COM5，Hash verified |
| 状态栏显示正确本地时间 | ⏸ 待测 | 校时后首页状态栏应显示北京时间；未校时应显示 --:-- |
| 分钟级刷新不闪烁 | ⏸ 待测 | 观察墨水屏无每秒重绘 |

## 4. 回滚方案

```bash
git revert 9551bd7
```

## 5. 申请审批事项

- [ ] **A. 全量接受** — 保留 commit，关闭本次评审循环
- [ ] **B. 退回修订** — 具体修订意见：________________
- [ ] **C. 部分接受** — 注明保留/回退项：________________

**审批人**（手写或电子签名）：________________
**审批日期**：________________
