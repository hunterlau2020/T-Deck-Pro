# 评审申请书：AI Text 聊天界面重构（第十九次申请）

- **申请人**：Claude（pda2 现场调试，配合用户实测按键）
- **申请日期**：2026-08-16
- **关联分支**：`HD-V2-250915`
- **关联 commit**（本轮整改，与文件名对应）：
  - `eecebda` — `pda2: restructure AI Text as a chat UI (history + input + side buttons)`
- **历史文档**（保留不覆盖）：前十八轮申请与结果见 `docs/reviews/`
- **硬件**：T-Deck-Pro HD-V2（V1.1，25-09-15 批次，COM5，**已连接、已烧录**）

---

## 1. 申请事由

用户要求把 AI Text 屏改为**标准聊天界面**（微信/WhatsApp 式）：上下两个多行文本框（上 2/3 只读历史、下 1/3 当前输入）、AI 回复左对齐 / 用户输入右对齐错开、历史可滚动、Send/Clear 缩小放在输入框侧边。

## 2. 变更明细（`examples/pda2/ui_ai_chat.cpp`，commit `eecebda`）

### 2.1 布局（wireframe）

```
┌──────────────────────────────┐
│ back "AI Text"               │
│ ┌──────────────────────────┐ │  164px（2/3）
│ │ 历史记录（只读可滚动）    │ │
│ │ ┌────────────────┐       │ │
│ │ │ AI 回复（左对齐）│       │ │
│ │ └────────────────┘       │ │
│ │       ┌────────────────┐ │ │
│ │       │ 用户输入（右对齐）│ │ │
│ │       └────────────────┘ │ │
│ └──────────────────────────┘ │
│ 状态行（Thinking.../错误）    │  16px
│ ┌──────────────────────┐┌──┐│
│ │ 当前输入（多行 82px）  ││Send│ 44px 侧栏
│ └──────────────────────┘│──┤│
│                        │Clear│
└──────────────────────────────┘
```

- 历史容器：`LV_SCROLLBAR_MODE_AUTO` + 垂直滚动（**触摸原生滚动** + Sym/Alt `+`/`-` 键盘滚动 ±120px）
- 气泡：白底黑边圆角，宽 178px，label 170px `LV_LABEL_LONG_WRAP`（LVGL 按 UTF-8 码点自动换行，替换了原固定 30 字节切行实现）；用户消息 `ALIGN_TOP_RIGHT`、AI 回复 `ALIGN_TOP_LEFT`
- 渲染后 `lv_obj_scroll_to_y(LV_COORD_MAX)` 自动滚到最新消息
- Send/Clear：44×38 小按钮竖排在输入框右侧；输入框 176×82 多行（上限 200 字符）

### 2.2 数据与行为

- 历史：`chat_msg_t[40]` 固定数组滚动存储（满 40 条丢最旧），**内存态**（重启清空，未做持久化——如需要可后续加 NVS/文件）
- 发送：用户消息**立即进历史**（聊天语义）；发送仍为异步任务 + 队列 + 页面代次（沿用上一轮范式）；**成功才清空输入框**，失败草稿保留可重试，AI 回复到达后追加进历史并滚动
- 键盘：`\n` 发送、`\b` 删字/空框退屏、`+`/`-` 滚历史、`\t`/`\v` 忽略；发送中吞键
- 删除的旧逻辑：分页浏览态（chat_viewing/page）、固定字节断行（chat_push_wrapped）、页内回答标签——全部由滚动历史替代

## 3. 验证状态

| 项目 | 状态 | 证据 |
|---|---|---|
| 编译 | ✅ 通过 | `pio run -e pda2` → SUCCESS |
| 烧录 | ✅ 完成 | COM5，Hash verified |
| 双框布局与按钮位置 | ⏸ 待测 | 历史 2/3 + 输入 1/3 + 右侧小按钮 |
| 消息左右对齐错开 | ⏸ 待测 | AI 左、用户右 |
| 历史滚动（触摸 / +/- 键） | ⏸ 待测 | 超过可视高度的对话可上下滚动 |
| 发送→历史追加→回复追加 | ⏸ 待测 | 串口 `[AIChat] reply len=...` |

## 4. 回滚方案

```bash
git revert eecebda
```

## 5. 申请审批事项

- [ ] **A. 全量接受** — 保留 commit，关闭本次评审循环
- [ ] **B. 退回修订** — 具体修订意见：________________
- [ ] **C. 部分接受** — 注明保留/回退项：________________

**审批人**（手写或电子签名）：________________
**审批日期**：________________
